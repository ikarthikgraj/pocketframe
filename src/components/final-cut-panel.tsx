"use client";
import { useRef, useState } from "react";
import { useRouter } from "next/navigation";
import type { RenderVersion, Scene } from "@/lib/db/repositories";
export function FinalCutPanel({ projectId, title, scenes, ready, render }: { projectId: string; title: string; scenes: Scene[]; ready: boolean; render?: RenderVersion }) {
  const router = useRouter(); const [working, setWorking] = useState(false); const [error, setError] = useState<string>(); const [subtitles, setSubtitles] = useState(false); const [music, setMusic] = useState<File>(); const musicRef = useRef<HTMLInputElement>(null);
  const approvedVoices = scenes.filter((scene) => scene.status === "TTS_APPROVED" || ["VIDEO_QUEUED", "VIDEO_GENERATING", "VIDEO_REVIEW", "APPROVED"].includes(scene.status)).length; const approvedScenes = scenes.filter((scene) => scene.approvedVersionId).length;
  async function renderTrailer() { setWorking(true); setError(undefined); const form = new FormData(); form.set("title", title); form.set("cta", "Listen now on Pocket FM"); form.set("subtitles", String(subtitles)); if (music) form.set("music", music); const response = await fetch(`/api/projects/${projectId}/render`, { method: "POST", body: form }); const data = await response.json().catch(() => ({})); setWorking(false); if (!response.ok) setError(data.error?.message ?? "Render failed."); else router.refresh(); }
  const status = render?.status ?? (ready ? "READY" : "NOT_READY");
  return <section className="export-gate"><h2>Final Cut</h2><p>Render status: <b>{status === "NOT_READY" ? "Not Ready" : status[0] + status.slice(1).toLowerCase()}</b></p><ul className="checklist"><li>{approvedVoices === scenes.length ? "✓" : "○"} Approved voice: {approvedVoices}/{scenes.length}</li><li>{approvedScenes === scenes.length ? "✓" : "○"} Approved scenes: {approvedScenes}/{scenes.length}</li><li>{ready ? "✓ Every scene has exactly one approved voice and video." : "○ Approve narration and one video version per scene."}</li></ul><p><b>Scene order:</b> {scenes.map((scene) => `Scene ${scene.sceneNumber}`).join(" → ") || "No scenes"}</p><label className="music-upload">Optional background music<input ref={musicRef} type="file" accept="audio/mpeg,audio/aac,audio/wav,audio/mp4,.mp3,.m4a,.aac,.wav" onChange={(event) => setMusic(event.target.files?.[0])} /><span>{music?.name ?? "No music selected"}</span></label><label className="subtitle-toggle"><input type="checkbox" checked={subtitles} onChange={(event) => setSubtitles(event.target.checked)} /> Burn subtitles</label><div className="actions"><button onClick={() => void renderTrailer()} disabled={!ready || working}>{working ? "Rendering…" : "Render Final Trailer"}</button></div>{error && <p className="error" role="alert">{error}</p>}{render?.status === "FAILED" && <p className="error" role="alert">{render.errorMessage}</p>}{render?.status === "COMPLETE" && <div className="final-player"><video controls preload="metadata" src={`/api/renders/${render.id}/video`} /><p>Final duration: {render.durationMs ? `${(render.durationMs / 1000).toFixed(1)} sec` : "—"}</p><a className="button" href={`/api/renders/${render.id}/video`} download={`pocketframe-final-v${render.versionNumber}.mp4`}>Download final trailer</a></div>}</section>;
}
